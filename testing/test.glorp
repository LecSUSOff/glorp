/* There is no point in this file
 * For testing purposes only
 * Might be changed in any time 
 */

fn null Main() {
    day = "Sunday"
    switch day {
        case "Sunday" or "Saturday" {
            out("It's the weekend!")
        }
        case "Monday" {
            out("Back to work.")
        }
    }
    else {
        out("It's a weekday.")
    }
}